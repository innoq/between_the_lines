Was wollen wir erreichen:

- m√∂glichst alle therapeutischen Einrichtungen in Deutschland erfassen
- f√Éºr jede Organisation wollen wir wissen, wo sie ist, wie sie hei√üt, wie die URL ist


Idee:

- Wir bauen eine Pipeline
- Daten erfassen (per Web-Crawler oder z.B. per Liste) -> die Daten validieren (stimmt z.B. die Adresse) -> die Daten nach bestimmten Kriterien taggen -> JSON produzieren -> in ein Endformat exportieren

MVP:
- Excel Liste rein -> Validieren -> Ergebnis der Validierung in das Excel schreiben -> erweitertes Excel ausgeben
